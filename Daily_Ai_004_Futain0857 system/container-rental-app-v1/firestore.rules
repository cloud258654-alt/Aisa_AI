rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Phase 1 MVP Security Rules: User must be authenticated to read or write
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
