import { describe, it } from 'vitest';

describe('Firestore Rules (Skipped)', () => {
  it.skip('skipping Firestore Rules unit testing as rules are deprecated in the Google Sheets backend migration', () => {
    // Empty block, test is skipped
  });
});
